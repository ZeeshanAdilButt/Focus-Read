{
  "name": "focus-read",
  "version": "1.0.0",
  "description": "Speed reader extension for Chrome",
  "scripts": {
    "build": "webpack --mode production",
    "build:release": "webpack --mode production && node scripts/prepare-release.js",
    "dev": "webpack --mode development --watch"
  },
  "dependencies": {
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^11.0.0",
    "sharp": "^0.34.5",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
